import ErrorReview from '@/components/error-review/ErrorReview';

export default function ErrorReviewTab() {
  return <ErrorReview />;
}

// TODO:
// each feedback is encapsulated in a card, basically just like that for now. can
