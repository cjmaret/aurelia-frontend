import GrammarReview from '@/components/grammar-review/GrammarReview';

export default function GrammarReviewTab() {
  return <GrammarReview />;
}

// TODO:
// each feedback is encapsulated in a card, basically just like that for now. can
